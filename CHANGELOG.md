# Changelog — CORTek Solar Designer (PVForge)

ทุกการแก้ไขจะเพิ่มเลขเวอร์ชันใน `pubspec.yaml` (รูปแบบ `major.minor.patch+build`)
และบันทึกไว้ในไฟล์นี้ เพื่อให้ย้อนกลับเวอร์ชันเก่าได้ถ้าลองแล้วไม่ถูกใจ

## 1.7.6+27 — 2026-08-27
### Fix
- หน้า "อินเวอร์เตอร์และอุปกรณ์" (`equipment_screen.dart`): ปุ่ม "เพิ่มเครื่องเอง"
  (FloatingActionButton) เดิมอยู่ตำแหน่งมาตรฐานมุมขวาล่างของจอ ซึ่งอยู่ใต้แถบ
  เมนูลอยด้านล่างพอดี (แถบเมนูวาดทับอยู่เหนือทุกหน้าอีกชั้นนอก Scaffold) ทำให้
  ปุ่มถูกบังจนมองไม่เห็น/กดไม่ได้ ตามที่แจ้ง ("ปุ่มเพิ่มย้ายมาอยู่บนแถบเมนู
  ตรงกลาง") แก้โดยยกปุ่มขึ้นให้พ้นพื้นที่แถบเมนู (`navReserve` เดียวกับที่ใช้
  แก้ปัญหานี้ในหน้าอื่นมาก่อน) ปัญหานี้เกิดเฉพาะหน้านี้เพราะเป็นหน้าเดียวใน
  แอปที่ใช้ `floatingActionButton`

## 1.7.5+26 — 2026-08-27
### UI
- ไอคอนติดตั้งแอป (`assets/branding/pvforge_app_icon_1024.png`) ตามที่ขอ
  ("พื้นหลัง logo ICON ให้พื้นหลังโปร่งใส") — เดิมไอคอนตัว squircle (สี่เหลี่ยม
  มุมมนไล่สีฟ้า) วางอยู่บนพื้นหลังสี่เหลี่ยมทึบสีน้ำเงินเข้มเต็มภาพ ลบพื้นหลัง
  ทึบรอบนอกออกให้โปร่งใส (ใช้ flood-fill จากขอบภาพไล่สีที่ใกล้เคียงพื้นหลังเดิม)
  เหลือแค่ตัวไอคอน squircle เอง — สร้างไฟล์ไอคอน Android ทั้ง 5 ขนาด
  (mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi) ใหม่จากไฟล์นี้อีกครั้ง

## 1.7.4+25 — 2026-08-27
### Fix
- **กู้คืนโปรเจกต์**: ตรวจพบว่าไฟล์บนเครื่องถูกย้อนกลับไปเป็นเวอร์ชัน 1.5.2+14
  โดยไม่ได้ตั้งใจ (คาดว่าเกิดจากการใช้งาน git/GitHub Desktop ไปลบ/ย้อนไฟล์เก่า)
  ทำให้งานทั้งหมดตั้งแต่เวอร์ชัน 1.5.3+15 ถึง 1.7.3+24 หายไปจากเครื่อง —
  กู้คืนโค้ดทั้งหมดกลับมาให้ครบ (เมนู 5 หัวข้อ + หน้าตั้งค่าเต็มจอ, ข้อมูล BOQ
  ที่เพิ่ม GPS/หลังคา/เฟสไฟฟ้า/รูปภาพ, การรวมแบนเนอร์แจ้งเตือนไปจุดเดียว,
  ไอคอน/ชื่อแอปฝั่ง Android, การเอาเส้นขอบขาวออกทั้งแอป และการเอาพื้นหลังกลับ
  เป็นไล่สีธรรมดา + เอาหัวข้อ "ภาพรวม" หน้าแรกออก) ให้ตรงกับที่เคยส่งไว้ทุก
  ประการ — **ข้อควรระวัง**: เพื่อป้องกันไม่ให้เหตุการณ์นี้เกิดซ้ำ ควรระมัดระวัง
  การใช้ git/GitHub Desktop (เช่น การ revert, reset, discard changes) บน
  โฟลเดอร์โปรเจกต์นี้ เพราะสามารถลบงานที่ยังไม่ได้ commit ทิ้งได้ทันทีโดยไม่มี
  การแจ้งเตือนล่วงหน้า

## 1.7.3+24 — 2026-08-27
### UI / Fix
- เอาหัวข้อ "ภาพรวม" (การ์ดสรุป 4 ช่องหน้าแรก) ออกทั้งหมดตามที่ขอ ลบทั้ง
  ส่วนหัวข้อ ปุ่ม "ทั้งหมด ⌄" และเมธอด `_overviewGrid()` ที่ไม่ได้ใช้แล้ว
  ออกจาก `home_screen.dart`
- พื้นหลังแอป: เปลี่ยนจากรูปภาพ (`pvforge_app_background.png`) กลับมาเป็น
  พื้นไล่สีขาวไปฟ้าธรรมดา (`LinearGradient`) ใน `main.dart` เพราะรูปภาพทำให้
  เห็นจอขาวก่อนสักครู่ทุกครั้งที่เปิดแอป (รอโหลดรูป) — พื้นไล่สีไม่ต้องโหลด
  ไฟล์ใด ๆ จึงขึ้นทันที ครอบคลุมทุกหน้าเหมือนเดิมเพราะตั้งไว้ระดับแอป
- พบและแก้จุดซ้ำซ้อน: `home_screen.dart` มีการวาดพื้นหลัง (รูป + ไล่สี) ของ
  ตัวเองซ้อนอยู่อีกชั้นนอกเหนือจากพื้นหลังระดับแอป — เอาชั้นซ้ำนี้ออก ให้ใช้
  พื้นหลังเดียวจาก `main.dart` เหมือนหน้าอื่นทั้งหมดในแอป

## 1.7.2+23 — 2026-08-27
### UI
- เอาเส้นขอบ/กรอบสีขาวออกเกือบทั้งแอปตามที่ขอ ("นำไปใช้ทุกเมนู ทุกกล่อง
  ข้อความ") พบต้นตอ 3 จุด แก้ที่ต้นทางเดียวทำให้ทุกหน้าที่ใช้วิดเจ็ตร่วมกันนี้
  หายขอบขาวไปพร้อมกัน:
  - `pvforge_theme.dart` — `CardThemeData.shape` มี `side` สีขาวกำหนดไว้
    ระดับธีมกลาง กระทบทุก `Card()` ทั้งแอป (เมนู "สร้างโปรเจกต์" 5 หัวข้อ,
    รายการโปรเจกต์, ประสิทธิภาพแผง/Inverter, สรุประบบ/BOQ, อุปกรณ์ ฯลฯ) —
    ลบ `side` ออก
  - `PVForgeCard` (`pvforge_components.dart`) — มีเงาขอบบนสีขาวซ้อนอยู่อีกชั้น
    — ลบออก เหลือแค่เงาสีฟ้าด้านล่าง
  - `AppBottomNavigation` — แถบเมนูล่างลอยมีกรอบขาวรอบตัว — ลบออก
  - จงใจไม่แตะเส้นขอบสีอื่นที่ใช้บอกสถานะ (เลือกอยู่/ผ่าน/ไม่ผ่าน) เพราะเป็น
    เส้นบอกสถานะจริง ไม่ใช่เส้นตกแต่งสีขาว

## 1.7.1+22 — 2026-08-27
### Fix
- เพิ่ม/แก้ไอคอนติดตั้งแอปและชื่อแอปที่ติดตั้งฝั่ง Android ให้ตรงกับ iOS
  (iOS มีไอคอนและชื่อ "PVForge" ครบอยู่แล้ว แต่ Android ยังเป็นไอคอน Flutter
  ค่าเริ่มต้นและชื่อแพ็กเกจดิบ "cortek_solar_designer") — สร้างไอคอน Android
  ครบ 5 ขนาด (mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi) จากไฟล์
  `assets/branding/pvforge_app_icon_1024.png` และเปลี่ยน `android:label` เป็น
  "PVForge" ใน `AndroidManifest.xml`

## 1.7.0+21 — 2026-08-27
### UI / Feature
- ปรับพื้นหลังแอปเป็นภาพถ่ายแผงโซลาร์ที่ส่งมา ครอบคลุมทุกหน้า (ตั้งค่าไว้
  ระดับแอปใน `main.dart` อยู่แล้ว จึงแค่แทนที่ไฟล์รูปเดียว) — *ภายหลังถูก
  ย้อนกลับเป็นพื้นไล่สีธรรมดาในเวอร์ชัน 1.7.3+24 เพราะโหลดช้า*
- ลดระยะห่างการ์ด "ภาพรวม" หน้าแรกให้ชิดกันพอประมาณ (mainAxisSpacing 10→6)
  และลด padding ของ `MetricCard` โหมดโปร่งใส — *ภายหลังกลายเป็นส่วนเกิน
  เพราะทั้งหัวข้อ "ภาพรวม" ถูกลบออกในเวอร์ชัน 1.7.3+24*
- รวมการแจ้งเตือนบันทึก/ลบ/แก้ไขทุกหน้าให้ใช้ฟังก์ชันกลางเดียวกัน
  `showAppBanner()` (ใน `pvforge_components.dart`) แสดงที่ด้านบนจอเสมอ (ไม่ใช้
  `SnackBar` ด้านล่างอีกต่อไป เพราะแถบเมนูลอยด้านล่างจะบังข้อความ) พร้อมพื้น
  หลังโปร่งใส (โทนเขียว=สำเร็จ/แดง=ผิดพลาด, alpha .55) ตามที่ขอ — เชื่อมทุกหน้า
  ที่เคยมีโค้ดแจ้งเตือนของตัวเองมาเรียกใช้ฟังก์ชันนี้แทน รวมถึงจุดที่เคยตกหล่น
  ไม่มีแบนเนอร์เลย เช่น "ลบโปรเจกต์แล้ว" และ "ลบรูปภาพแล้ว"

## 1.6.1+20 — 2026-08-27
### Feature
- นำข้อมูลใหม่จากเมนู 5 หัวข้อ (พิกัด GPS, รูปแบบหลังคา, ระบบไฟฟ้า, รูปภาพ
  หน้างาน) ไปแสดงในรายงาน PDF/BOQ ตามที่ขอ: เพิ่มแถว "พิกัด GPS" (แสดงเมื่อมี
  พิกัด), "รูปแบบหลังคา" (แสดงเมื่อระบุไว้), "ระบบไฟฟ้า" (แสดงเสมอ ค่าเริ่มต้น
  "1 เฟส") ในตารางข้อมูลลูกค้า และเพิ่มส่วนใหม่ "รูปภาพหน้างาน (N รูป)" ท้าย
  รายงาน แสดงรูปที่ถ่าย/เลือกไว้เป็นตาราง (แสดงเฉพาะเมื่อมีรูปอย่างน้อย 1 รูป)

## 1.6.0+19 — 2026-08-27
### Feature
- **ชุดฟีเจอร์ใหญ่**: ทำเมนูย่อย 4 จาก 5 หัวข้อในหน้า "สร้างโปรเจกต์" ที่เคย
  เป็นแค่ข้อความแจ้งเฉย ๆ ให้ใช้งานได้จริง:
  - **สถานที่ติดตั้ง** (`location_map_screen.dart`) — ช่องกรอกที่อยู่ + แผนที่
    (OpenStreetMap ผ่าน `flutter_map`) แตะปักหมุดตำแหน่งติดตั้งได้ บันทึกพิกัด
    ละติจูด/ลองจิจูด
  - **ข้อมูลหลังคา** (`roof_info_screen.dart`) — เลือกรูปแบบหลังคา (จั่ว/ปั้นหยา/
    เพิงหมาแหงน/แบน/มะนิลา/ทรงผสม) เป็นกริดไอคอนเลือกได้
  - **ระบบไฟฟ้า** (`electrical_system_screen.dart`) — เลือกเฟสไฟฟ้า (1 เฟส/
    3 เฟส) เป็นการ์ดเลือกได้
  - **รูปภาพหน้างาน** (`site_photos_screen.dart`) — ถ่ายรูป/เลือกรูปจากคลัง
    บันทึกไฟล์ถาวรในเครื่อง แสดงเป็นกริดพร้อมลบรายรูปได้ (ใช้งานได้เฉพาะบน
    Android/iOS เท่านั้น เนื่องจากปลั๊กอินกล้องไม่รองรับ Windows)
  - เพิ่ม dependency ใหม่: `flutter_map`, `latlong2`, `image_picker`,
    `path_provider` — **ต้องรัน `flutter pub get` ก่อนบิลด์ครั้งถัดไป**
  - เพิ่มสิทธิ์ที่จำเป็นฝั่ง Android (`CAMERA`, `INTERNET`,
    `READ_MEDIA_IMAGES`, `READ_EXTERNAL_STORAGE`) และ iOS
    (`NSCameraUsageDescription`, `NSPhotoLibraryUsageDescription`)
- **หน้าตั้งค่าเปลี่ยนจากป๊อปอัปเป็นหน้าเต็มจอ** (`settings_screen.dart`)
  ตามที่ขอ ("เมนูตั้งค่า ให้สร้างเป็นหน้าเหมือนเมนูอื่น") เนื้อหาเดิมทั้งหมด
  (Company Workspace, DEMO, ประสิทธิภาพแผง & Inverter, เลือกโทนสีแอป) ย้ายมา
  อยู่ในหน้านี้ เปิดแบบหน้าปกติเหมือนเมนูอื่นในแถบเมนูล่าง

## 1.5.6+18 — 2026-08-26
### UI
- ปรับหน้าเข้าสู่ระบบ: เอากรอบการ์ด (`Card`) รอบฟอร์มออก ให้ฟอร์มลอยอยู่บนพื้น
  หลังโดยตรง และเปลี่ยนปุ่ม "ทดลองใช้ DEMO โดยไม่สมัครสมาชิก" จากปุ่มแบบมีกรอบ
  เต็มความกว้าง เป็นปุ่มข้อความเล็ก ๆ "demo" ลอยมุมขวาล่างของจอแทน

## 1.5.5+17 — 2026-08-26
### Fix
- ย้ายข้อความแจ้งเตือน "บันทึกและคำนวณใหม่" ในหน้าประสิทธิภาพแผง/Inverter
  จาก `SnackBar` ด้านล่าง (ถูกแถบเมนูลอยด้านล่างบังจนมองไม่เห็น) ไปเป็น
  `MaterialBanner` ที่ด้านบนจอแทน (หายเองใน 2 วินาที) — ปัญหาแบบเดียวกับที่
  แก้ไปแล้วในเวอร์ชัน 1.5.3+15

## 1.5.4+16 — 2026-08-26
### Fix
- เชื่อมหน้า "ประสิทธิภาพแผง & Inverter" (`efficiency_loss_screen.dart`) ที่
  มีอยู่แล้วในโค้ดแต่ไม่เคยถูกเรียกใช้จากเมนูไหนเลย (โค้ดตาย) เข้ากับเมนู
  ตั้งค่า เปิดพร้อมข้อมูลโปรเจกต์ล่าสุดที่บันทึกไว้
- แก้บั๊กที่พบระหว่างเชื่อมหน้านี้: ปุ่ม "บันทึกและคำนวณใหม่" เดิมไม่ได้บันทึก
  ข้อมูลลงเครื่องจริง (แก้แค่ในหน่วยความจำชั่วคราว) ทำให้ข้อมูลหายเมื่อปิดแอป
  — แยกเป็นอัปเดตตัวเลขสด (ทุกครั้งที่พิมพ์) กับบันทึกจริงลงเครื่อง (เมื่อกด
  ปุ่มเท่านั้น) ให้ชัดเจน

## 1.5.3+15 — 2026-08-26
### Fix
- แก้ปัญหาเนื้อหาหน้าแรกโดนแถบเมนูลอยด้านล่างบัง/ทับกันบนโทรศัพท์จริง (แถบ
  เมนูลอยอยู่นอกโครงสร้าง Scaffold ปกติ ทำให้หน้าจอที่คำนวณความสูงเต็มจอไม่รู้
  ว่ามีพื้นที่ท้ายจอถูกแถบเมนูบังอยู่จริง ๆ) — คำนวณพื้นที่ที่ถูกแถบเมนูบังแล้ว
  หักออกจากความสูงที่ใช้จัดวางเนื้อหาหน้าแรก ปัญหานี้อาจเกิดซ้ำได้กับหน้าอื่น
  ที่จัดวางเนื้อหาเต็มความสูงจอในลักษณะเดียวกัน

## 1.5.2+14 — 2026-08-26
### Fix
- การ์ด "ภาพรวม" หน้าแรก (`MetricCard`): เดิมไอคอนวงกลม+ตัวเลขทั้งกลุ่มถูก
  จัดกึ่งกลาง (`Center`) ทำให้ตำแหน่งไอคอนของแต่ละการ์ดขยับไปคนละที่ตาม
  ความยาวตัวเลข (เช่น "1" กับ "87792" ทำให้ไอคอนอยู่คนละตำแหน่ง) แก้เป็น
  จัดชิดซ้ายเสมอ (`Align(alignment: Alignment.centerLeft)`) ทำให้ไอคอนของ
  ทุกการ์ดในกริดอยู่ตำแหน่ง/ระยะย่อหน้าเดียวกันเป๊ะ ไม่ขยับตามความยาวตัวเลข

## 1.5.1+13 — 2026-08-26
### Fix
- โดม Peak Sun Hour บนกราฟการใช้ไฟ 24 ชั่วโมง เดิมยอดโดมเตี้ยกว่าที่ตั้งใจ
  (จุดพระอาทิตย์ลอยอยู่เหนือเส้นโค้งจริง) สาเหตุคือใช้เส้นโค้งเบซิเยร์
  ควอดราติกซึ่งจะพีคแค่ครึ่งทางระหว่างเส้นฐานกับจุดควบคุมเท่านั้น ไม่ใช่ที่
  จุดควบคุมจริง — แก้โดยเปลี่ยนไปวาดเป็นครึ่งวงรีจริงแทน ทำให้ยอดโดมสูงเท่ากับ
  ระดับที่ "ค่าสูงสุดในแต่ละวัน" ของกราฟใช้ไฟไปถึงพอดี ตามที่ขอ

## 1.5.0+12 — 2026-08-26
### Feature
- กราฟการใช้ไฟ 24 ชั่วโมง (`lib/widgets/daily_load_chart.dart`) ตอนนี้วาด
  "โดม Peak Sun Hour" สีส้มทับอยู่ด้วย — กึ่งกลางที่เวลา 12:00 (เที่ยงวัน)
  แล้วขยายออกสองข้างเท่า ๆ กันตามจำนวนชั่วโมงแดดที่ตั้งไว้ (เช่น Peak Sun
  Hour 4 ชม. จะแสดงเป็นโดมตั้งแต่ 10:00–14:00) พร้อมจุดพระอาทิตย์ตรงกลาง
  และป้ายกำกับค่าปัจจุบันในแถบตำนานของกราฟ อัปเดตสดทันทีเมื่อลากปรับค่าที่
  กราฟ Peak Sun Hour ด้านล่าง
- ย้ายกราฟ Peak Sun Hour (และช่องกรอกตัวเลขของมัน) มาไว้ต่อจากกราฟการใช้ไฟ
  24 ชั่วโมงทันทีในหน้า `project_screen.dart` (เดิมอยู่ห่างออกไปหลังช่อง
  แบตเตอรี่) เพื่อให้เห็นความเชื่อมโยงระหว่างสองกราฟชัดเจนขึ้น

## 1.4.1+11 — 2026-08-26
### Fix
- **Build error**: `lib/widgets/peak_sun_hour_chart.dart(32,23): error: Not a constant expression`
  ตอนบิลด์ Windows — สาเหตุคือไฟล์นี้ใช้วิดเจ็ต `GradientIconBadge` (จาก
  `pvforge_components.dart`) แต่ลืม import ไฟล์นั้นเข้ามา แก้โดยเพิ่ม
  `import 'pvforge_components.dart';` เข้าไป

## 1.4.0+10 — 2026-08-26
### UI / Feature
- กราฟการใช้ไฟ 24 ชั่วโมง (`lib/widgets/daily_load_chart.dart`) ในหน้า
  ข้อมูลหลังคา/ระบบไฟฟ้า: เอาแถบเลื่อน (`Slider`) ด้านล่างกราฟออกทั้งหมด
  ตามที่ขอ เปลี่ยนมาให้ **ลากบนตัวกราฟโดยตรง** (แตะแล้วลาก) เพื่อปรับค่าไฟ
  ของแต่ละชั่วโมงแทน — ค่าที่ได้ยังคงถูกรวมเป็นไฟกลางวัน/กลางคืนแล้วส่งไป
  อัปเดตช่องกรอก "ไฟกลางวัน/วัน" และ "ไฟกลางคืน/วัน" ทันทีเหมือนเดิม
- แก้บั๊กที่พบระหว่างทำงานนี้: เดิมถ้าผู้ใช้แก้ "ไฟกลางวัน/วัน" หรือ
  "ไฟกลางคืน/วัน" จากช่องกรอกตัวเลขโดยตรง (ไม่ได้ลากกราฟ) รูปกราฟจะไม่ขยับ
  ตาม ตอนนี้กราฟจะรีเซ็ตรูปตามค่าล่าสุดให้อัตโนมัติทุกครั้งที่ค่าสองช่องนี้
  เปลี่ยนจากภายนอก
- เพิ่มกราฟใหม่สำหรับ **Peak Sun Hour** (`lib/widgets/peak_sun_hour_chart.dart`)
  เป็นแท่งกราฟลากปรับค่าได้ (0–8 ชม./วัน) พร้อมไอคอนพระอาทิตย์และตัวเลขค่า
  ปัจจุบัน วางไว้เหนือช่องกรอก Peak Sun Hour เดิมในหน้า `project_screen.dart`
  (ยังคงช่องกรอกตัวเลขไว้ให้พิมพ์ค่าละเอียดได้ ทั้งสองจุดซิงค์กันสองทาง)

## 1.3.0+9 — 2026-08-26
### UI
- ปรับดีไซน์ไอคอนให้เป็นภาษาเดียวกันทั้งแอปตามที่ขอ ("ทุกหน้า ทุกหัวข้อ")
  เพิ่มวิดเจ็ตกลาง `GradientIconBadge` (วงกลมไล่สี เงานุ่ม ใส่ไอคอนหรือ
  ตัวอักษรย่อได้) ใน `lib/widgets/pvforge_components.dart` แล้วแทนที่
  `CircleAvatar`/ไอคอนสีเทาแบบเดิมทุกจุดที่พบ ได้แก่:
  - หน้ารายชื่อโปรเจกต์ (`projects_screen.dart`) — ไอคอนคนหน้ารายการ
  - หน้าอุปกรณ์ (`equipment_screen.dart`) — ไอคอนรายการ + ไอคอนหน้าว่างเปล่า
  - หน้าสรุประบบ/BOQ (`summary_screen.dart`) — ไอคอนหัวข้อรายการ BOQ และ
    การ์ดสถิติ (`_stat`) 3 ช่อง (kWp, จำนวนแผง, ผลผลิต)
  - หน้า Company Workspace (`workspace_screen.dart`) — ไอคอนโลโก้บริษัท และ
    ตัวอักษรย่อชื่อสมาชิกแต่ละคน
  - เมนูตั้งค่า/DEMO ใน `main.dart` (บอตทอมชีต "เลือกโทนสีแอป")
  - หน้า DEMO (`demo_screen.dart`) — ไอคอนตัวอักษรย่อรายการ
  - หน้ารายละเอียดโปรเจกต์ (`project_screen.dart`) — เมนูย่อยทั้ง 5 หัวข้อ
    (สถานที่ติดตั้ง, หลังคา, ระบบไฟฟ้า, จุดติดตั้ง/ระยะสาย, รูปภาพหน้างาน)
    ใช้สีต่างกันตามหมวด
  - หน้าผลการคำนวณ (`result_screen.dart`) — ไอคอนหัวข้อ "ระบบแบตเตอรี่"
  - หน้าเลือก Boost/MPPT (`boost_mppt_selection_screen.dart`) — ไอคอนท้าย
    รายการแต่ละรุ่น
  ตรวจสอบแล้วว่าหน้า `auth_screen.dart` และ `efficiency_loss_screen.dart`
  ไม่มีไอคอน/CircleAvatar ที่ต้องปรับ จึงไม่แก้ไข
- หมายเหตุ: ยังไม่ได้ทดสอบรันจริงหลังแก้ชุดนี้ — ถ้าลองแล้วดูยาก/ไม่ถูกใจ
  สามารถย้อนกลับไปเวอร์ชัน 1.2.2+8 ได้ (โค้ดก่อนหน้านี้ถูกเก็บไว้ใน git
  history/backup ตามปกติ)

## 1.2.2+8 — 2026-08-26
### UI
- การ์ด "ภาพรวม" หน้าแรก: เอากรอบขาว/เงาออกให้โปร่งใส กลืนกับพื้นหลัง
- ย้ายไอคอนวงกลมไล่สีมาไว้ข้างหน้าตัวเลข (แนวนอน) แทนแบบเดิมที่อยู่ด้านบน

## 1.2.1+7 — 2026-08-26
### UI
- การ์ด "ภาพรวม" หน้าแรก: เอาป้ายข้อความ (label) ออก เหลือแค่ไอคอน + ตัวเลข
  จัดกึ่งกลาง, ไอคอนเป็นวงกลมไล่สี (ยังมีข้อความเดิมเป็น tooltip ตอนชี้ค้าง)

## 1.2.0+6 — 2026-08-26
### Fix
- `SolarProject.recommendedPanels` เพิ่ม `manualPanelCount` — ตอนนี้ปรับ
  "แผง/String" หรือ "จำนวน String" เองในหน้าจัด String แล้ว ค่าจะซิงค์ไปทุก
  หน้า (kWp, พื้นที่หลังคา, ผลผลิตไฟ, สรุประบบ/BOQ) ค่านี้จะถูกล้างอัตโนมัติ
  ทุกครั้งที่เปลี่ยนรุ่นแผงใหม่ที่หน้าเลือกแผง
### Feature
- ปุ่ม "สร้างรายงาน PDF" ในหน้าสรุประบบใช้งานได้จริงแล้ว (เดิมเป็นแค่
  ข้อความ "จะเพิ่มในเวอร์ชันถัดไป") ดึงข้อมูลโปรเจกต์จริงทั้งหมดมาสร้าง PDF
  ผ่านไฟล์ใหม่ `lib/services/pdf_report_service.dart` — เพิ่ม dependency
  `pdf` และ `printing` ใน `pubspec.yaml`

## 1.1.2+5 — 2026-08-26
### UI
- แผงที่ยังไม่มีรูปจริง (Jinko/Trina/Astronergy รูปซ้ำกัน หรือแผงที่ผู้ใช้
  เพิ่มเอง) ใช้รูปตัวแทนแบบเดียวกันทั้งหมด (ไอคอนแผงบนพื้นไล่สีฟ้า) แทนที่จะ
  ไล่สีต่างกันไปตามยี่ห้อ

## 1.1.1+4 — 2026-08-26
### Fix
- คืนรูปจริงให้ LONGi และ JA Solar ในแคตตาล็อกแผง (ถูกปิดไว้ระหว่างไล่บั๊ก
  ค้าง แล้วลืมเปิดกลับหลังแก้บั๊กเสร็จ)
- หน้า "โปรเจกต์ของฉัน" ตอนไม่มีโปรเจกต์เลย เดิมเป็นหน้าว่างเปล่า
  (`SizedBox.expand()`) เพิ่มภาพประกอบ + ข้อความ + ปุ่ม "สร้างโปรเจกต์ใหม่"
### UI
- การ์ด `MetricCard` แก้บั๊กที่รับพารามิเตอร์ไอคอนมาแต่ไม่ยอมโชว์ (โชว์
  สัญลักษณ์ยูนิโค้ด ▣☀↗฿ แทน) ให้โชว์ไอคอนจริงในวงกลมสี
- ไอคอนแถบเครื่องมือหน้าแรก (`_tools()`) เปลี่ยนจากตัวอักษรในกล่องเป็นไอคอน
  Material จริง

## 1.1.0+3 — 2026-08-26
### Fix
- แยกหน้า "เลือก Boost / MPPT" ออกจากหน้า "จัดเรียง String / ตรวจสอบ MPPT"
  (ไฟล์ใหม่ `lib/screens/boost_mppt_selection_screen.dart`) ระหว่างแยกพบว่า
  เดิมหน้าจัด String จะเขียนทับค่า Inverter ที่เพิ่งเลือกในสาย On-grid/Hybrid
  ด้วยค่า Boost/MPPT default ของตัวเองเงียบ ๆ — แก้ไขแล้ว

## 1.0.1+2 — 2026-08-26
### Fix
- **บั๊กร้ายแรง**: แอปค้างและ crash ("Lost connection to device") ทุกครั้งที่
  เข้าสายแอร์โซล่าเซลล์แล้วกดถัดไปจากหน้าเลือกแผง สาเหตุคือ
  `lib/screens/string_checker_screen.dart` มี for-loop ที่ขาด `++`
  (`for (var index = 0; index < boostOptions.length; index)`) ทำให้เป็นลูป
  ไม่รู้จบตอนสร้าง dropdown เลือก Boost/MPPT จนหน่วยความจำเต็มและแอป crash
  โดยไม่มี exception ใด ๆ ให้เห็นเลย

## 1.0.0+1 — เวอร์ชันเริ่มต้นก่อนเริ่มบันทึก Changelog
