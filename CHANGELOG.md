# Changelog — CORTek Solar Designer (PVForge)

ทุกการแก้ไขจะเพิ่มเลขเวอร์ชันใน `pubspec.yaml` (รูปแบบ `major.minor.patch+build`)
และบันทึกไว้ในไฟล์นี้ เพื่อให้ย้อนกลับเวอร์ชันเก่าได้ถ้าลองแล้วไม่ถูกใจ

## 1.5.2+14 — 2026-08-26
### Fix
- การ์ด "ภาพรวม" หน้าแรก (`MetricCard`): เดิมไอคอนวงกลม+ตัวเลขทั้งกลุ่มถูก
  จัดกึ่งกลาง (`Center`) ทำให้ตำแหน่งไอคอนของแต่ละการ์ดขยับไปคนละที่ตาม
  ความยาวตัวเลข (เช่น "1" กับ "87792" ทำให้ไอคอนอยู่คนละตำแหน่ง) แก้เป็น
  จัดชิดซ้ายเสมอ (`Align(alignment: Alignment.centerLeft)`) ทำให้ไอคอนของ
  ทุกการ์ดในกริดอยู่ตำแหน่ง/ระยะย่อหน้าเดียวกันเป๊ะ ไม่ขยับตามความยาวตัวเลข

## 1.5.1+13 — 2026-08-26
### Fix
- โดม Peak Sun Hour บนกราฟการใช้ไฟ 24 ชั่วโมง เดิมยอดโดมเตี้ยกว่าที่ตั้งใจ
  (จุดพระอาทิตย์ลอยอยู่เหนือเส้นโค้งจริง) สาเหตุคือใช้เส้นโค้งเบซิเยร์
  ควอดราติกซึ่งจะพีคแค่ครึ่งทางระหว่างเส้นฐานกับจุดควบคุมเท่านั้น ไม่ใช่ที่
  จุดควบคุมจริง — แก้โดยเปลี่ยนไปวาดเป็นครึ่งวงรีจริงแทน ทำให้ยอดโดมสูงเท่ากับ
  ระดับที่ "ค่าสูงสุดในแต่ละวัน" ของกราฟใช้ไฟไปถึงพอดี ตามที่ขอ

## 1.5.0+12 — 2026-08-26
### Feature
- กราฟการใช้ไฟ 24 ชั่วโมง (`lib/widgets/daily_load_chart.dart`) ตอนนี้วาด
  "โดม Peak Sun Hour" สีส้มทับอยู่ด้วย — กึ่งกลางที่เวลา 12:00 (เที่ยงวัน)
  แล้วขยายออกสองข้างเท่า ๆ กันตามจำนวนชั่วโมงแดดที่ตั้งไว้ (เช่น Peak Sun
  Hour 4 ชม. จะแสดงเป็นโดมตั้งแต่ 10:00–14:00) พร้อมจุดพระอาทิตย์ตรงกลาง
  และป้ายกำกับค่าปัจจุบันในแถบตำนานของกราฟ อัปเดตสดทันทีเมื่อลากปรับค่าที่
  กราฟ Peak Sun Hour ด้านล่าง
- ย้ายกราฟ Peak Sun Hour (และช่องกรอกตัวเลขของมัน) มาไว้ต่อจากกราฟการใช้ไฟ
  24 ชั่วโมงทันทีในหน้า `project_screen.dart` (เดิมอยู่ห่างออกไปหลังช่อง
  แบตเตอรี่) เพื่อให้เห็นความเชื่อมโยงระหว่างสองกราฟชัดเจนขึ้น

## 1.4.1+11 — 2026-08-26
### Fix
- **Build error**: `lib/widgets/peak_sun_hour_chart.dart(32,23): error: Not a constant expression`
  ตอนบิลด์ Windows — สาเหตุคือไฟล์นี้ใช้วิดเจ็ต `GradientIconBadge` (จาก
  `pvforge_components.dart`) แต่ลืม import ไฟล์นั้นเข้ามา แก้โดยเพิ่ม
  `import 'pvforge_components.dart';` เข้าไป

## 1.4.0+10 — 2026-08-26
### UI / Feature
- กราฟการใช้ไฟ 24 ชั่วโมง (`lib/widgets/daily_load_chart.dart`) ในหน้า
  ข้อมูลหลังคา/ระบบไฟฟ้า: เอาแถบเลื่อน (`Slider`) ด้านล่างกราฟออกทั้งหมด
  ตามที่ขอ เปลี่ยนมาให้ **ลากบนตัวกราฟโดยตรง** (แตะแล้วลาก) เพื่อปรับค่าไฟ
  ของแต่ละชั่วโมงแทน — ค่าที่ได้ยังคงถูกรวมเป็นไฟกลางวัน/กลางคืนแล้วส่งไป
  อัปเดตช่องกรอก "ไฟกลางวัน/วัน" และ "ไฟกลางคืน/วัน" ทันทีเหมือนเดิม
- แก้บั๊กที่พบระหว่างทำงานนี้: เดิมถ้าผู้ใช้แก้ "ไฟกลางวัน/วัน" หรือ
  "ไฟกลางคืน/วัน" จากช่องกรอกตัวเลขโดยตรง (ไม่ได้ลากกราฟ) รูปกราฟจะไม่ขยับ
  ตาม ตอนนี้กราฟจะรีเซ็ตรูปตามค่าล่าสุดให้อัตโนมัติทุกครั้งที่ค่าสองช่องนี้
  เปลี่ยนจากภายนอก
- เพิ่มกราฟใหม่สำหรับ **Peak Sun Hour** (`lib/widgets/peak_sun_hour_chart.dart`)
  เป็นแท่งกราฟลากปรับค่าได้ (0–8 ชม./วัน) พร้อมไอคอนพระอาทิตย์และตัวเลขค่า
  ปัจจุบัน วางไว้เหนือช่องกรอก Peak Sun Hour เดิมในหน้า `project_screen.dart`
  (ยังคงช่องกรอกตัวเลขไว้ให้พิมพ์ค่าละเอียดได้ ทั้งสองจุดซิงค์กันสองทาง)

## 1.3.0+9 — 2026-08-26
### UI
- ปรับดีไซน์ไอคอนให้เป็นภาษาเดียวกันทั้งแอปตามที่ขอ ("ทุกหน้า ทุกหัวข้อ")
  เพิ่มวิดเจ็ตกลาง `GradientIconBadge` (วงกลมไล่สี เงานุ่ม ใส่ไอคอนหรือ
  ตัวอักษรย่อได้) ใน `lib/widgets/pvforge_components.dart` แล้วแทนที่
  `CircleAvatar`/ไอคอนสีเทาแบบเดิมทุกจุดที่พบ ได้แก่:
  - หน้ารายชื่อโปรเจกต์ (`projects_screen.dart`) — ไอคอนคนหน้ารายการ
  - หน้าอุปกรณ์ (`equipment_screen.dart`) — ไอคอนรายการ + ไอคอนหน้าว่างเปล่า
  - หน้าสรุประบบ/BOQ (`summary_screen.dart`) — ไอคอนหัวข้อรายการ BOQ และ
    การ์ดสถิติ (`_stat`) 3 ช่อง (kWp, จำนวนแผง, ผลผลิต)
  - หน้า Company Workspace (`workspace_screen.dart`) — ไอคอนโลโก้บริษัท และ
    ตัวอักษรย่อชื่อสมาชิกแต่ละคน
  - เมนูตั้งค่า/DEMO ใน `main.dart` (บอตทอมชีต "เลือกโทนสีแอป")
  - หน้า DEMO (`demo_screen.dart`) — ไอคอนตัวอักษรย่อรายการ
  - หน้ารายละเอียดโปรเจกต์ (`project_screen.dart`) — เมนูย่อยทั้ง 5 หัวข้อ
    (สถานที่ติดตั้ง, หลังคา, ระบบไฟฟ้า, จุดติดตั้ง/ระยะสาย, รูปภาพหน้างาน)
    ใช้สีต่างกันตามหมวด
  - หน้าผลการคำนวณ (`result_screen.dart`) — ไอคอนหัวข้อ "ระบบแบตเตอรี่"
  - หน้าเลือก Boost/MPPT (`boost_mppt_selection_screen.dart`) — ไอคอนท้าย
    รายการแต่ละรุ่น
  ตรวจสอบแล้วว่าหน้า `auth_screen.dart` และ `efficiency_loss_screen.dart`
  ไม่มีไอคอน/CircleAvatar ที่ต้องปรับ จึงไม่แก้ไข
- หมายเหตุ: ยังไม่ได้ทดสอบรันจริงหลังแก้ชุดนี้ — ถ้าลองแล้วดูยาก/ไม่ถูกใจ
  สามารถย้อนกลับไปเวอร์ชัน 1.2.2+8 ได้ (โค้ดก่อนหน้านี้ถูกเก็บไว้ใน git
  history/backup ตามปกติ)

## 1.2.2+8 — 2026-08-26
### UI
- การ์ด "ภาพรวม" หน้าแรก: เอากรอบขาว/เงาออกให้โปร่งใส กลืนกับพื้นหลัง
- ย้ายไอคอนวงกลมไล่สีมาไว้ข้างหน้าตัวเลข (แนวนอน) แทนแบบเดิมที่อยู่ด้านบน

## 1.2.1+7 — 2026-08-26
### UI
- การ์ด "ภาพรวม" หน้าแรก: เอาป้ายข้อความ (label) ออก เหลือแค่ไอคอน + ตัวเลข
  จัดกึ่งกลาง, ไอคอนเป็นวงกลมไล่สี (ยังมีข้อความเดิมเป็น tooltip ตอนชี้ค้าง)

## 1.2.0+6 — 2026-08-26
### Fix
- `SolarProject.recommendedPanels` เพิ่ม `manualPanelCount` — ตอนนี้ปรับ
  "แผง/String" หรือ "จำนวน String" เองในหน้าจัด String แล้ว ค่าจะซิงค์ไปทุก
  หน้า (kWp, พื้นที่หลังคา, ผลผลิตไฟ, สรุประบบ/BOQ) ค่านี้จะถูกล้างอัตโนมัติ
  ทุกครั้งที่เปลี่ยนรุ่นแผงใหม่ที่หน้าเลือกแผง
### Feature
- ปุ่ม "สร้างรายงาน PDF" ในหน้าสรุประบบใช้งานได้จริงแล้ว (เดิมเป็นแค่
  ข้อความ "จะเพิ่มในเวอร์ชันถัดไป") ดึงข้อมูลโปรเจกต์จริงทั้งหมดมาสร้าง PDF
  ผ่านไฟล์ใหม่ `lib/services/pdf_report_service.dart` — เพิ่ม dependency
  `pdf` และ `printing` ใน `pubspec.yaml`

## 1.1.2+5 — 2026-08-26
### UI
- แผงที่ยังไม่มีรูปจริง (Jinko/Trina/Astronergy รูปซ้ำกัน หรือแผงที่ผู้ใช้
  เพิ่มเอง) ใช้รูปตัวแทนแบบเดียวกันทั้งหมด (ไอคอนแผงบนพื้นไล่สีฟ้า) แทนที่จะ
  ไล่สีต่างกันไปตามยี่ห้อ

## 1.1.1+4 — 2026-08-26
### Fix
- คืนรูปจริงให้ LONGi และ JA Solar ในแคตตาล็อกแผง (ถูกปิดไว้ระหว่างไล่บั๊ก
  ค้าง แล้วลืมเปิดกลับหลังแก้บั๊กเสร็จ)
- หน้า "โปรเจกต์ของฉัน" ตอนไม่มีโปรเจกต์เลย เดิมเป็นหน้าว่างเปล่า
  (`SizedBox.expand()`) เพิ่มภาพประกอบ + ข้อความ + ปุ่ม "สร้างโปรเจกต์ใหม่"
### UI
- การ์ด `MetricCard` แก้บั๊กที่รับพารามิเตอร์ไอคอนมาแต่ไม่ยอมโชว์ (โชว์
  สัญลักษณ์ยูนิโค้ด ▣☀↗฿ แทน) ให้โชว์ไอคอนจริงในวงกลมสี
- ไอคอนแถบเครื่องมือหน้าแรก (`_tools()`) เปลี่ยนจากตัวอักษรในกล่องเป็นไอคอน
  Material จริง

## 1.1.0+3 — 2026-08-26
### Fix
- แยกหน้า "เลือก Boost / MPPT" ออกจากหน้า "จัดเรียง String / ตรวจสอบ MPPT"
  (ไฟล์ใหม่ `lib/screens/boost_mppt_selection_screen.dart`) ระหว่างแยกพบว่า
  เดิมหน้าจัด String จะเขียนทับค่า Inverter ที่เพิ่งเลือกในสาย On-grid/Hybrid
  ด้วยค่า Boost/MPPT default ของตัวเองเงียบ ๆ — แก้ไขแล้ว

## 1.0.1+2 — 2026-08-26
### Fix
- **บั๊กร้ายแรง**: แอปค้างและ crash ("Lost connection to device") ทุกครั้งที่
  เข้าสายแอร์โซล่าเซลล์แล้วกดถัดไปจากหน้าเลือกแผง สาเหตุคือ
  `lib/screens/string_checker_screen.dart` มี for-loop ที่ขาด `++`
  (`for (var index = 0; index < boostOptions.length; index)`) ทำให้เป็นลูป
  ไม่รู้จบตอนสร้าง dropdown เลือก Boost/MPPT จนหน่วยความจำเต็มและแอป crash
  โดยไม่มี exception ใด ๆ ให้เห็นเลย

## 1.0.0+1 — เวอร์ชันเริ่มต้นก่อนเริ่มบันทึก Changelog
