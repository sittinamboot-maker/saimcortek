// เวอร์ชันแอปปัจจุบัน แสดงไว้ที่หน้าล็อกอินให้เช็กง่าย ๆ ว่าเครื่องนี้รันโค้ด
// เวอร์ชันล่าสุดจริงหรือไม่ (ป้องกันปัญหาแบบที่เคยเกิด — ไฟล์บนเครื่องถูกย้อน
// กลับไปเป็นเวอร์ชันเก่าโดยไม่รู้ตัว)
//
// **ต้องอัปเดตค่านี้ให้ตรงกับ `version:` ใน pubspec.yaml ทุกครั้งที่บั๊ม
// เวอร์ชัน** (ตามธรรมเนียมเดิมของโปรเจกต์นี้ที่ต้องบั๊มเวอร์ชัน + เพิ่ม
// CHANGELOG.md ทุกครั้งที่แก้โค้ดอยู่แล้ว)
const String kAppVersion = '1.7.7+28';
